//Criando Array com construtor

const newArray = new Array() // Criamos uma lista
console.log(newArray) // Se observamos o log, veremos que é '[]', logo se tratando de um array

// [] Array
// {} Object

const avaible = new Array(10)
console.log(avaible.length) // Nesse caso, conseguimos criar um array com posições ja pré definidas, embora estejam vazias.