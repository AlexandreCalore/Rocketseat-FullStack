# Sobre a aula.

A ideia aqui é separar as funcionalidades do projeto em módulos, e não deixar tudo em um unico arquivo.
Precisando declarar tanto no módulo que irá exportar, o que iremos exportar (export), import no módulo que vai receber, e no html, definir o type para "module"