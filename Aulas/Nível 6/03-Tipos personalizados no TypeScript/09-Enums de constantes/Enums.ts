// Enum = para nomear constantes

enum Profile {
    Admin = 1,
    Client = 2,
    Seller = 3
} // Nomeamos e criamos constantes 

let profile: number = 1 // teoricamente seria o admin

let profile2: number = Profile.Client // Acessamos a constante criada.